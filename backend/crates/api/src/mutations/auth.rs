// Auth mutations (login, register) have been moved to REST endpoints.
// See the tinyboards_auth crate: POST /api/v2/auth/login and /api/v2/auth/register.
// This module is intentionally empty — auth no longer goes through GraphQL.
